
'use server';

/**
 * @fileOverview This file is deprecated. AtlasBurn now uses deterministic production SDK ingestion.
 */

export async function runSleekSandboxTest() {
  return { success: false, error: "Synthetic injection is deactivated. Connect a production SDK feed." };
}
